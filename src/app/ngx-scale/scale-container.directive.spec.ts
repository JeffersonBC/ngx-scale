import { ScaleContainerDirective } from './scale-container.directive';

describe('NgxScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleContainerDirective();
    expect(directive).toBeTruthy();
  });
});
